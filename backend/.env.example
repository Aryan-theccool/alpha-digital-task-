# Database connection string (PostgreSQL for production / hosted, or SQLite for local offline)
DATABASE_URL=postgresql://alpha:alphapassword@localhost:5432/digital_alpha

# Server configuration
PORT=8000
HOST=0.0.0.0
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rewards configuration
COIN_EARN_RATE_INR=100
MAX_COIN_CAP_PER_TXN=50
